import 'package:flutter/material.dart';

const primaryColor = Color(0xFF3580FF);

const textField = Color(0xFF848A94);

// Light Theme
const secondaryColorLightTheme = Color(0xFFE9F1FF);
const bgColorLightTheme = Colors.white;
const textColorLightTheme = Color(0xFF002055);
const textBodyColorLightTheme = Color(0xFF868D95);

// Dark Theme
const secondaryColorDarkTheme = Color(0xFF191D30);
const bgColorDarkTheme = Color(0xFF0A0C16);
const textColorDarkTheme = Colors.white;
const textBodyColorDarkTheme = Color(0xFF868D95);
